package array_;

import java.math.BigDecimal;
import java.util.Scanner;

public class Ex10810 {
	
	public static void main(String[] args) {
		//풀이 1
//		Scanner sc = new Scanner(System.in);
//		BigDecimal A =  new BigDecimal(sc.next());
//		BigDecimal B =  new BigDecimal(sc.next());
//		System.out.println(A.add(B));
//		sc.close();
		
		//풀이 2
		Scanner sc = new Scanner(System.in);
		String[] A = sc.next().split("");
		String[] B = sc.next().split("");
		
		String[] biggerOne;
		String[] smallerOne;
		boolean differ = false;
		int start = 0;
		
		if(A.length > B.length) {
			biggerOne = A;
			smallerOne = B;
			differ = true;
			start = A.length - B.length;
		} else if (A.length == B.length) {
			biggerOne = A;
			smallerOne = B;
		} else {
			biggerOne = B;
			smallerOne = A;
			differ = true;
			start = B.length - A.length;
		}
		
		int[] result = new int[biggerOne.length+1];
		
		if (differ) {
			for (int i = 0; i < start; i++) {
				result[i+1] = Integer.parseInt(biggerOne[i]);
			} 
		} else {
			int gap = Integer.parseInt(biggerOne[0]) + Integer.parseInt(smallerOne[0]);
		}
		
		for (int i = start; i < biggerOne.length; i++) {
			int a = Integer.parseInt(biggerOne[i]);
			int b = Integer.parseInt(smallerOne[i-start]);
			
			
		}
		
		sc.close();
	}

}
//BigDecimal : long보다 큰 범위의 수 셈할 때 사용
