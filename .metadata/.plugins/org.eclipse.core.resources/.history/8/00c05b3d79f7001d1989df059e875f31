package _programers;

import java.util.HashMap;

public class 신고결과받기 {

	public static void main(String[] args) {
		String[] id_list = {"muzi", "frodo", "apeach", "neo"};
		String[] report = {"muzi frodo","apeach frodo","frodo neo","muzi neo","apeach muzi"};
		int k = 2;
 		System.out.println(solution(id_list, report, k));
	}

	private static int[] solution(String[] id_list, String[] report, int k) {
		int[] answer = new int[id_list.length];
		
		HashMap<String, Integer> map = new HashMap<>();
		
		//신고횟수 세기 위한 amp
		for (String id : id_list) {
			map.put(id, 0);
		}
		
		for (int i = 0; i < answer.length; i++) {
			String user = report[i].split(" ")[0];
			String reporter = report[i].split(" ")[1];
			boolean noReport = true;
			for (String string : report) { //이전에 신고했었는지 확인
				if(string.contains(user) && string.contains(reporter)) {
					noReport = false;
					break;
				}
			}
		}
		
		
		
		
		return null;
	}

}
